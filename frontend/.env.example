# Backend API base URL. Leave UNSET for local dev — Vite proxies /api to :8000
# (see vite.config.js). Set only for a deployed frontend, e.g.:
# VITE_API_URL=https://orbital-guardian-api.onrender.com
VITE_API_URL=
