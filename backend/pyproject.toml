[project]
name = "app"
version = "0.1.0"
dependencies = [ "fastapi", "uvicorn[standard]", "sqlalchemy", "pydantic", "pydantic-settings", "python-jose[cryptography]", "passlib[bcrypt]", "bcrypt", "requests", "sgp4", "httpx", "psycopg2-binary", "python-dotenv", "pytest", "email-validator" ]
requires-python = "~=3.12.0"
